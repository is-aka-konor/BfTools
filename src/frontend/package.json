{
  "name": "bfsitegen-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "mac-preview": "npm run build && (rsync -a --ignore-existing ../../dist-site/ ./dist/ || cp -R -n ../../dist-site/* ./dist/) && npm run preview",
    "test:unit": "vitest run --coverage",
    "test:unit:watch": "vitest",
    "test:e2e": "node scripts/run-playwright.js",
    "test:ci": "vitest run --coverage && node scripts/run-playwright.js --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "@tailwindcss/vite": "^4.1.12",
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/user-event": "^14.5.2",
    "@types/node": "^24.3.0",
    "@vitest/coverage-v8": "^2.0.5",
    "axe-core": "^4.10.0",
    "daisyui": "^5.0.50",
    "fake-indexeddb": "^6.0.1",
    "jsdom": "^24.1.0",
    "msw": "^2.3.1",
    "playwright": "^1.47.2",
    "tailwindcss": "^4.1.12",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  },
  "dependencies": {
    "dexie": "^4.0.8",
    "lit": "^3.1.0",
    "minisearch": "^6.3.0",
    "navigo": "^8.11.1",
    "workbox-window": "^7.1.0"
  }
}
